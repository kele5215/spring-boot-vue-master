import Vue from 'vue'
import { Card, Form, FormItem, Input, Button, Message } from 'element-ui'
Vue.use(Card)
Vue.use(Form)
Vue.use(FormItem)
Vue.use(Input)
Vue.use(Button)

Vue.use(Message)

Vue.prototype.$message = Message
